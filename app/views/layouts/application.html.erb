<!DOCTYPE html>
<html>
  <head>
    <title>Kelutral</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if current_user %>
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <%= link_to "Kelutral", root_path, class: "navbar-brand", style: "margin-left: 20px" %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Expressions", expressions_path, class: "nav-link"%>
            </li>
            <li class="nav-item">
              <%= link_to "Words", words_path, class: "nav-link"%>
            </li>
            <li class="nav-item">
              <%= link_to "Phrases", phrases_path, class: "nav-link"%>
            </li>
          </ul>
        </div>
      </nav>
    <% end %>
    <%= yield %>
  </body>
</html>
