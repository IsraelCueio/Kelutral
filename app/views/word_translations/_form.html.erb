<%= form_with(model: word_translation) do |form| %>
  <% if word_translation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(word_translation.errors.count, "error") %> prohibited this word_translation from being saved:</h2>

      <ul>
        <% word_translation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
