<%= form_with(model: expression) do |form| %>
  <% if expression.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(expression.errors.count, "error") %> prohibited this expression from being saved:</h2>

      <ul>
        <% expression.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% if false %>
    <div>
      <%= form.label :word_id, style: "display: block" %>
      <%= form.collection_select :word_id, Word.order(:id), :id, :name %>
    </div>

    <div>
      <%= form.label :phrase_id, style: "display: block" %>
      <%= form.collection_select :phrase_id, Phrase.order(:id), :id, :text %>
    </div>

  <% end %>

      <label for="fname">Frase:</label>
      <input type="text" id="teste" onkeydown="GetValue(this.value,event,false)" onblur="GetValue(this.value,event,true)"><br><br>

      <div>
        <%= form.hidden_field :word_json, id: 'json_sender' %>
      <div>
    <%= form.fields_for :word, Word.new do |word_attributes|%>
        <div>
          <%= word_attributes.hidden_field :name, id: 'new_words' %>
        </div>
    <% end %>

    <div>
      <%= form.fields_for :phrase, Phrase.new do |phrase_attributes|%>

        <div>
          <%= phrase_attributes.hidden_field :text, id: 'new_phrase' %>
        </div>

        <br>
      <% end %>
    </div>

  <div>
    <br>
    <%= form.submit %>
  </div>
<% end %>
<br>