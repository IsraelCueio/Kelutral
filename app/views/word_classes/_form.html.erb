<%= form_with(model: word_class) do |form| %>
  <% if word_class.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(word_class.errors.count, "error") %> prohibited this word_class from being saved:</h2>

      <ul>
        <% word_class.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
