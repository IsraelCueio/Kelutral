<%= form_with(model: word) do |form| %>
  <% if word.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
        <% word.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :word_class_id, style: "display: block" %>
    <%= form.collection_select :word_class_id, WordClass.order(:id), :id, :name %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :audio, style: "display: block" %>
    <%= form.text_field :audio %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
